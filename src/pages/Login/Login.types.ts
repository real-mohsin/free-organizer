export interface LoginFormValues {

    /**
     * User email address.
     */
    email: string;

    /**
     * User password.
     */
    password: string;

    /**
     * Keep the user signed in.
     */
    rememberMe: boolean;

}